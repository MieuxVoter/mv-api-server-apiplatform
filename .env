
# DO NOT EDIT THIS FILE.
# COPY IT TO .env.local
#   cp .env .env.local
# AND EDIT THE LATTER.

# In all environments, the following files are loaded if they exist,
# the later taking precedence over the former:
#
#  * .env                contains default values for the environment variables needed by the app
#  * .env.local          uncommitted file with local overrides
#  * .env.$APP_ENV       committed environment-specific defaults
#  * .env.$APP_ENV.local uncommitted environment-specific overrides
#
# Real environment variables win over .env files.
#
# DO NOT DEFINE PRODUCTION SECRETS IN THIS FILE NOR IN ANY OTHER COMMITTED FILES.
#
# Run "composer dump-env prod" to compile .env files for production use (requires symfony/flex >=1.2).
# https://symfony.com/doc/current/best_practices/configuration.html#infrastructure-related-configuration


MVOAS_SERVER=https://oas.mieuxvoter.fr
# If in dev and using for example
#     symfony serve --port 8008
# Use this:
#MVOAS_SERVER=https://localhost:8008

# These are used by our docker-compose 'db' container, a mariadb.
# You can ignore these if you are using sqlite in the DATABASE_URL below,
# or not using docker-compose at all (in dev locally, for example).
# You HAVE TO change at least the password on prod instances when using a mysql database.
MVOAS_MYSQL_DATABASE=limaju
MVOAS_MYSQL_USERNAME=limaju
MVOAS_MYSQL_PASSWORD=REDEFINE_ME_OR_SUFFER_CONSEQUENCES


###> symfony/framework-bundle ###
APP_ENV=dev
APP_SECRET=9075ad55bb91bc2d3c822f6c195ab01e
#TRUSTED_PROXIES=127.0.0.1,127.0.0.2
#TRUSTED_HOSTS='^localhost|example\.com$'
###< symfony/framework-bundle ###

###> nelmio/cors-bundle ###
# Replace `localhost` by your domain name (eg: oas.mieuxvoter.fr)
CORS_ALLOW_ORIGIN=^https?://localhost(:[0-9]+)?$
###< nelmio/cors-bundle ###

###> doctrine/doctrine-bundle ###
# Format described at https://www.doctrine-project.org/projects/doctrine-dbal/en/latest/reference/configuration.html#connecting-using-a-url
# For an SQLite database, use: "sqlite:///%kernel.project_dir%/var/data.db"
# Configure your db driver and server_version in config/packages/doctrine.yaml
# DATABASE_URL=mysql://limaju:limaju@db:3306/limaju
DATABASE_URL=sqlite:///%kernel.project_dir%/var/data.db
###< doctrine/doctrine-bundle ###

###> lexik/jwt-authentication-bundle ###
# You HAVE TO re-generate new keys (see README) and change the password.
JWT_SECRET_KEY=%kernel.project_dir%/config/jwt/private.pem
JWT_PUBLIC_KEY=%kernel.project_dir%/config/jwt/public.pem
JWT_PASSPHRASE=3a25d422a7bce186ad01ab6ef8efddfe
###< lexik/jwt-authentication-bundle ###

###> symfony/messenger ###
# Choose one of the transports below, or not.
# MESSENGER_TRANSPORT_DSN=amqp://guest:guest@localhost:5672/%2f/messages
# MESSENGER_TRANSPORT_DSN=doctrine://default
# MESSENGER_TRANSPORT_DSN=redis://localhost:6379/messages
###< symfony/messenger ###

# Used when publishing generated client libraries.
# Ignore this variable if you're not going to do that.
# The `export` statement allows us to `source` this file, and dotenv is smart enough to ignore it.
export GIT_TOKEN=PUT_YOUR_GIT_TOKEN_FROM_YOUR_GITHUB_USER_SETTINGS
